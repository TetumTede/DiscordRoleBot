# Bot token from https://discord.com/developers/applications -> your app -> Bot -> Reset Token
DISCORD_TOKEN=

# Application (client) ID from the same app's General Information page
CLIENT_ID=

# Optional: set this to a server ID while developing so slash commands
# register instantly in that one server instead of taking up to an hour
# to appear globally. Leave blank to register commands globally.
GUILD_ID=
